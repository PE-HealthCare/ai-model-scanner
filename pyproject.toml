[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-model-scanner"
version = "0.1.0"
description = "Detection of steganographic malware hidden in AI model weights"
requires-python = ">=3.10"
